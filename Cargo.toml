[package]
name = "displayflow"
version = "0.2.0"
edition = "2021"
authors = ["piot5"]
description = "DisplayFlow: Windows Display & DDC/CI Management (Config-based Profiles)"

[dependencies]
windows = { version = "0.52", features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_UI_HiDpi",
    "Win32_Devices_Display",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_System_LibraryLoader",
    "Win32_System_Threading",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }
log = "0.4"
anyhow = "1.0"
thiserror = "1.0"
winreg = "0.52"
ctrlc = "3.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
clap = { version = "4.0", features = ["derive"] }

# displaymanager crates from crates.io
# Using published crates instead of internal unsafe bindings
df_ddc = "0.1.1"
df_displmgr = "0.1.1"
df_displmgr_info = "0.1.1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
